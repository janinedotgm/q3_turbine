
import { sendMessage } from "../../utils/telegramApi";
import { findGameById } from "../../db/queries/game";
import { findUsersByIds } from "../../db/queries/users";

export const notifyGameEnd = async (round: any, telegramId: string) => {

    
    
}